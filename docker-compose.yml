services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    environment:
      - HF_HOME=/app/data/hf_cache
    env_file:
      - .env
    restart: unless-stopped
