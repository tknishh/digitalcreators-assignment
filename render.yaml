services:
  - type: web
    name: video-stitcher-api
    runtime: docker
    plan: free
    healthCheckPath: /health
    envVars:
      - key: DEBUG
        value: "false"
      - key: LOW_MEMORY_MODE
        value: "true"
    disk:
      name: video-data
      mountPath: /app/data
      sizeGB: 1
